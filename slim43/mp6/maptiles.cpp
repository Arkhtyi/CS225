/**
 * @file maptiles.cpp
 * Code for the maptiles function.
 */
	 			
#include <iostream>
#include <map>
#include "maptiles.h"

using namespace std;

MosaicCanvas * mapTiles(SourceImage const & theSource, vector<TileImage> const & theTiles)
{
    /**
     * @todo Implement this function!
     */
  /*
    MosaicCanvas* canvas = new MosaicCanvs(theSource.getRows(), theSource.getColumns());
    
    RGBAPixel sourceColor;
    RGBAPixel tileColor;
    
    for(int i = 0; i < theSource.getColumns(); i++){
    	for(int j = 0; j < theSource.getRows(); j++){
    		sourceColor = theSource.getRegionColor(j,i);\
    		for(int z = 0; z < theTiles.size(); z++){
    			
    		}
    		
    	}
    }
    */
     
	return NULL;
}
